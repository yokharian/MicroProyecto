export default {
	headparser: require('./headParser').default,
	// filemeta: require('./filemeta').default,
	// timestamp: require('./timestamp').default,
	// tracker: require('./tracker').default,
	// urlshort: require('./urlshort').default,
};
